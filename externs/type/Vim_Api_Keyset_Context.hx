package externs.type;

/**
	```lua
	(class) vim.api.keyset.context
	```
**/
@:native("vim.api.keyset.context") extern class Vim_Api_Keyset_Context {
	/**
		```lua
		(field) vim.api.keyset.context.types: string[]?
		```
	**/
	extern var types : Null<Array<String>>;
}