package nvim.type.uv.tcp_bind;

/**
	```lua
	(class) uv.tcp_bind.flags
	```
	
	---
	
	 uv.tcp_bind.flags
	
**/
@:structInit class Flags {
	/**
		```lua
		(field) uv.tcp_bind.flags.ipv6only: boolean
		```
	**/
	extern public var ipv6only : Bool;
}